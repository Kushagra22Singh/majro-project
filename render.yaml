services:
  - type: web
    name: leaflens-ml-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT api:app
    autoDeploy: true
